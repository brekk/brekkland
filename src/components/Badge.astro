---
import "@/components/Badge.scss"
import blem from "#/utilities/blem"
const bem = blem("Badge")
const { m: mods = [] } = Astro.props
---

<div class={bem("", mods)}>
  <slot name="icon" />
  <slot />
</div>
